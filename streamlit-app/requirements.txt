# Pinning versions to avoid dependency resolution issues and using CPU-only torch.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.3.1
torchvision==0.18.1
streamlit==1.35.0
requests==2.31.0
sentence-transformers==2.7.0
chromadb==0.5.0
